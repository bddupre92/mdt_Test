apiVersion: v1
kind: Service
metadata:
  name: mdt-api
  labels:
    app: mdt-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: mdt-api
